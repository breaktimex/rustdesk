name: Full Flutter CI

on:
  # Automatic triggers disabled; run manually from the Actions tab only.
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
